# Minimal requirements to run the RiG backend and ingestion scripts.
# Note: some packages (faiss, ifcopenshell) require platform-specific wheels or extra steps.
# Install in a Python 3.11+ virtualenv. Example:
# python -m venv .venv
# source .venv/bin/activate
# pip install -U pip setuptools wheel
# pip install -r requirements.txt

fastapi>=0.95.0
uvicorn[standard]>=0.22.0
pydantic>=1.10.0
python-dotenv>=1.0.0
numpy>=1.24.0
pandas>=2.0.0
neo4j>=5.7.0
sentence-transformers>=2.2.2
scikit-learn>=1.2.0
faiss-cpu>=1.7.4  # or faiss-gpu if you have CUDA available; see Faiss install docs
groq>=0.1.0  # groq python client
requests>=2.31.0
pillow>=9.5.0
pytest>=7.4.0

# Background processing and job queues
celery>=5.3.0
redis>=4.5.0
flower>=2.0.0  # Celery monitoring

# Cloud storage and direct uploads
boto3>=1.26.0  # AWS S3/R2 compatible
botocore>=1.29.0

# Database for job tracking
sqlalchemy>=2.0.0
alembic>=1.8.0
psycopg2-binary>=2.9.0  # PostgreSQL adapter

# File processing and validation
python-magic>=0.4.27
# tus-js-client>=0.0.1  # For resumable uploads

# WebSocket support for real-time updates
websockets>=11.0.0
fastapi-websocket>=0.0.1

# IFC tools (optional; required for ingest scripts that parse IFC)
# If you need ifcopenshell, use a platform wheel or conda package. Example (macOS/conda):
# conda install -c conda-forge ifcopenshell
# Or pip if a wheel exists for your platform:
ifcopenshell>=0.8.0

# LLM for NL-to-SPARQL (OpenAI)
openai>=1.0.0

# RDF and GraphDB support
rdflib>=6.0.0
pyshacl>=0.23.0  # SHACL validation
SPARQLWrapper>=1.8.6  # SPARQL queries
requests>=2.31.0  # For GraphDB REST API

# JSON-LD support
pyld>=2.0.3  # JSON-LD processing

# Development / utility
black>=24.3.0
isort>=6.0.0
flake8>=6.0.0

# If you plan to run the frontend build commands from Python tasks, node and npm are required (install separately)
